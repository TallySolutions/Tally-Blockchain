volumes:
  ${ORDERER_HOST}.${DOMAIN}:

networks:
  tally_network:
    name: tally_network

services:

  ${ORDERER_HOST}.${DOMAIN}:
    container_name: ${ORDERER_HOST}.${DOMAIN}_${DOCKER_ORDERER_PORT}
    image: hyperledger/fabric-orderer:latest
    labels:
      service: hyperledger-fabric
    environment:
      - FABRIC_LOGGING_SPEC=INFO
      - ORDERER_GENERAL_LISTENADDRESS=0.0.0.0
      - ORDERER_GENERAL_LISTENPORT=${ORDERER_PORT}
      - ORDERER_GENERAL_LOCALMSPID=${ORDERER_MSPID}
      - ORDERER_GENERAL_LOCALMSPDIR=/var/hyperledger/orderer/msp
      # enabled TLS
      - ORDERER_GENERAL_TLS_ENABLED=true
      - ORDERER_GENERAL_TLS_PRIVATEKEY=/var/hyperledger/orderer/tls/server.key
      - ORDERER_GENERAL_TLS_CERTIFICATE=/var/hyperledger/orderer/tls/server.crt
      - ORDERER_GENERAL_TLS_ROOTCAS=[/var/hyperledger/orderer/tls/ca.crt]
      - ORDERER_GENERAL_CLUSTER_CLIENTCERTIFICATE=/var/hyperledger/orderer/tls/server.crt
      - ORDERER_GENERAL_CLUSTER_CLIENTPRIVATEKEY=/var/hyperledger/orderer/tls/server.key
      - ORDERER_GENERAL_CLUSTER_ROOTCAS=[/var/hyperledger/orderer/tls/ca.crt]
      - ORDERER_GENERAL_BOOTSTRAPMETHOD=none
      - ORDERER_CHANNELPARTICIPATION_ENABLED=true
      - ORDERER_ADMIN_TLS_ENABLED=true
      - ORDERER_ADMIN_TLS_CERTIFICATE=/var/hyperledger/orderer/tls/server.crt
      - ORDERER_ADMIN_TLS_PRIVATEKEY=/var/hyperledger/orderer/tls/server.key
      - ORDERER_ADMIN_TLS_ROOTCAS=[/var/hyperledger/orderer/tls/ca.crt]
      - ORDERER_ADMIN_TLS_CLIENTROOTCAS=[/var/hyperledger/orderer/tls/ca.crt]
      - ORDERER_ADMIN_LISTENADDRESS=0.0.0.0:${ORDERER_ADMIN_PORT}
      - ORDERER_OPERATIONS_LISTENADDRESS=0.0.0.0:${ORDERER_OPS_PORT}
      - ORDERER_METRICS_PROVIDER=prometheus
    working_dir: /root
    command: orderer
    volumes:
        - ${ORDERER_HOME}/msp:/var/hyperledger/orderer/msp
        - ${ORDERER_HOME}/tls/:/var/hyperledger/orderer/tls
        - ${ORDERER_HOST}.${DOMAIN}:/var/hyperledger/production/orderer
    ports:
      - ${DOCKER_ORDERER_PORT}:${ORDERER_PORT}
      - ${DOCKER_ORDERER_ADMIN_PORT}:${ORDERER_ADMIN_PORT}
      - ${DOCKER_ORDERER_OPS_PORT}:${ORDERER_OPS_PORT}
    networks:
      - tally_network
